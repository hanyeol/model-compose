controller:
  adapter:
    type: http-server
    port: 8080
    base_path: /api
  webui:
    driver: gradio
    port: 8081

workflow:
  title: Translate Text
  description: Translate the input text into another language using a pretrained seq2seq translation model.
  job:
    input: ${input}
    output:
      translated: ${output}

component:
  type: model
  task: text-to-text
  driver: huggingface
  model: alirezamsh/small100
  max_concurrent_count: 1
  action:
    text: ${input.text as text}
    max_input_length: 1024
    params:
      do_sample: false
